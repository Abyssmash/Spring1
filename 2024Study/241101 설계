1. html 요소구성
2. 동적 데이터 처리 (JS)
html의 동적 데이터 처리를 하기 위해 자바 스크립트를 사용했다.

* 먼저 만드는 순서를 잘 짜야한다.

241031 로그인 화면 만들기 예시
JS 순서
1. 아이디에 입력된 값을 가져온다.
기술 > getelementID
2. 비밀번호에 입력된 값을 가져온다.
기술 > getelementID
3. 아이디가 8글자 이상인지 유효성 체크
기술 > 자료형과 조건문
4. 비번이 4글자 이상인지 유효성 체크
기술 > 자료형과 조건문

241031 계산기 만들기 예시
JS 순서
1. 키를 클릭하면 text수식으로 만들어짐
 a. 키를 클릭하면 JS 호출
 b. 호출된 함수는 키의 값을 가져옴
 c. 가져온 키로 text 수식을 만든다.
2. 키 중에 '='를 클릭하면 수식을 계산한다.

innerHTML : 태그를 넣을 수 있음
innerText : 텍스트를 가져옴